name: Shell Commands
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: hollo
        run: echo "my name is narasimha"
      - name: multiline
        run:
          npm -v
          node -v